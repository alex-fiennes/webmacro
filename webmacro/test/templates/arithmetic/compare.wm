# Testing comparison between different types of integers

#set $i2 = 2
#set $i1 = 1
#set $i0 = 0
#set $in1 = -1
#set $in2 = -2

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i < $j : #if ($i < $j) { Yes } #else { No } } }

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i <= $j : #if ($i <= $j) { Yes } #else { No } } }

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i == $j : #if ($i == $j) { Yes } #else { No } } }

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i > $j : #if ($i > $j) { Yes } #else { No } } }

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i >= $j : #if ($i >= $j) { Yes } #else { No } } }

#foreach $i in $ints {  
  #foreach $j in $ints {
    $i != $j : #if ($i != $j) { Yes } #else { No } } }

