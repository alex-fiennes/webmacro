<project name="wiki" default="compile" basedir=".">
	<property name="app.name" value="wiki" />
	<property name="dir.class" value="./class" />
	<property name="dir.src"   value="./src" />
	<property name="dir.lib"   value="./lib" />
        <property name="dir.templates" value="./templates" />
        <property name="dir.etc"       value="./etc" />
	<property name="dir.htdocs"    value="./htdocs" />
	<property name="dir.dist"      value="./wiki" />

	<property name="file.jar"  value="./wiki.jar" />
	<property name="file.resources" value="./wiki-resources.jar" />

	<path id="path.class">
		<fileset dir="${dir.lib}">
			<include name="*.jar" />
			<include name="*.zip" />
		</fileset>
	</path>


	<target name="prepare">
		<mkdir dir="${dir.class}" />
	</target>

	<target name="clean">
		<delete dir="${dir.class}" />
		<delete dir="${dir.dist}" />
		<delete file="${file.jar}" />
		<delete file="${file.resources}" />
	</target>

	<target name="compile" depends="prepare">
		<javac  destdir="${dir.class}"
			debug="off"
			optimize="on"
			deprecation="off"
		>
			<src path="${dir.src}" />
			<classpath refid="path.class" />
		</javac>
	</target>

	<target name="jar" depends="prepare,compile">
		<jar 	jarfile="${file.jar}"
			basedir="${dir.class}"
		/>
	</target>

	<target name="resources">
		<jar	jarfile="${file.resources}"
			basedir="${dir.templates}"
		/>
	</target>

        <target name="dist" depends="prepare,compile,jar,resources">
		<mkdir dir="${dir.dist}" />
		<mkdir dir="${dir.dist}/WEB-INF/lib" />
		<copy todir="${dir.dist}">
			<fileset dir="${dir.htdocs}" />
		</copy>
		<copy todir="${dir.dist}/WEB-INF/lib">
			<fileset dir="${dir.lib}" />
			<fileset dir="./" includes="*.jar" />
		</copy>
		<copy todir="${dir.dist}/WEB-INF">
			<fileset dir="${dir.etc}" />
		</copy>
	</target>

	<target name="all" depends="clean,prepare,compile,jar,resources,dist">

	</target>

</project>
